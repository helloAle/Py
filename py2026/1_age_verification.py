# igual ou maior que 18 anos

idade = int(input('Qual sua idade?    '))
verificador = idade >= 18
# print(verificador)

# OPERDORES LOGICOS
# and or not
carteira = True
verificador = idade >= 18 and carteira
print(verificador)

# cnh = input('Possui carteira de motorista? | sim - 0 | não - 1 |    ')
# verificador = idade >= 18 and (cnh == '0' or cnh == 'sim')
# print(verificador)
